---
import "@fontsource/mononoki";
import { Icon } from "astro-icon/components";

const platforms = [
    {service: "GPG", value: "0000000000000000", icon_size: 27},
    {service: "Email", value: "john.pork@example.com", icon_size: 27},
    {service: "Discord", value: "john.porn#9999", icon_size: 27},
]
---

{platforms.map((item) => (
<div class:list={["handle-pill", "pill-" + item.service.toLowerCase()]}>
    <Icon name={item.service.toLowerCase()} size={item.icon_size} />
    <div class="handle-pill-content">
        <p>{item.value}</p>
        <button><Icon name="copy" size={18} /></button>
    </div>
</div>
))}

<script>
    document.querySelectorAll(".handle-pill").forEach((e) => {
        e?.querySelector("button")?.addEventListener("click", () => {
            navigator.clipboard.writeText(e.querySelector("p")!.innerHTML)
        });
    });
</script>

<style lang="scss">
    .handle-pill {
        display: flex;
        align-items: center;
        color: var(--text-color);
        margin: 10px;
        margin-left: 13px;
        font-family: "Mononoki", monospace;
        font-size: 1.1rem;

        .handle-pill-content {
            display: inherit;
            align-items: inherit;
            justify-content: center;
            background-color: #242424;
            border: 1px dashed #555555;
            border-radius: 0.6rem;
            padding: 5px;
            margin-left: 10px;

            p { padding: 0 5px }
        }

        &.pill-gpg, &.pill-discord {
            p {
                padding-top: 1px;
            }
        }

        button {
            border: none;
            display: inherit;
            justify-content: inherit;
            align-items: inherit;
            height: 28px;
            aspect-ratio: 1;
            border-radius: 0.4rem;
            background-color: #454545;
            margin-left: 3px;

            svg { color: var(--offwhite-color) }

            &:hover {
                background-color: #595959;
                cursor: pointer;
            }

            &:active {
                transform: scale(0.95);
            }
        }
    }
</style>
